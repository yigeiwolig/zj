# 高度标定问题诊断（精简调试版）

## ⚠️ Flash空间限制

ATmega328P只有30KB Flash，启用完整调试会超出限制。已改为**最小调试版本**。

## 问题现象

1. **自动标定失败**：
   - 日志只显示一条 `[高度命令] base mm=245`
   - 没有看到 `[高度命令] danger mm=XXX`
   - 固件持续回读旧值 `DGA:1640`, `DGB:64`

2. **手动写入错误**：
   - 写入 `16.8 cm`
   - 固件返回 `6.8 cm` (68mm instead of 168mm)
   - 小程序日志显示：`expect=168 incoming=68` → 不匹配

## 已修改的文件

### 1. firmware/F3 max/f3_max_servo.ino

✅ **已启用 `F3_BLE_CMD_DEBUG = 1`（精简版）**

**精简后的日志格式（单行输出）：**

```
[DA16.8->[mm=168]OK A]     // 危险高度写入成功
[TB24.5->[mm=245]OK B]     // 检测高度写入成功
```

格式说明：
- `[命令->` - 接收到的BLE命令（如 `DA16.8`）
- `[mm=XXX]` - 解析后的毫米值
- `OK A]` - 危险高度保存成功
- `OK B]` - 检测高度保存成功

如果解析失败，不会有任何输出（说明命令被拒绝）。

### 2. miniprogram/package-app/pages/scan/scan.js

小程序调试日志保持不变：

**自动标定流程 (`_f3CalCommitHeightWrites`)**:
- `[自动标定] 开始写入: base=245mm, danger=XXXmm`
- `[自动标定] 步骤1: 写入base 245mm`
- `[自动标定] base命令已发送，等待2.8s`
- `[自动标定] 步骤2: 写入danger XXXmm`
- `[自动标定] danger命令已发送，等待2.5s后完成`

**写入函数 (`_f3SubmitHeightMm`)**:
- `[_f3SubmitHeightMm] 开始: kind=danger, storageUnits=168`
- `[_f3SubmitHeightMm] cmd=DA16.8`
- `[_f3SubmitHeightMm] _sendF3HeightBleCmd返回: true`

**BLE发送函数 (`_sendF3HeightBleCmd`)**:
- `[_sendF3HeightBleCmd] 最终命令: DA16.8#`
- `📤 [蓝牙] 危险高度 发送「DA16.8#」(16.8 cm)`
- `[_sendF3HeightBleCmd] 实际发送命令: DA16.8#` (会打印2次)

## 重新烧录固件

**必须重新烧录才能看到调试输出！**

1. 打开 Arduino IDE（或 Mixly）
2. 打开文件：`firmware/F3 max/f3_max_servo.ino`
3. 选择板型：Arduino Nano, ATmega328P
4. 点击「上传」
5. 等待上传完成

**预期编译大小**：应该在29KB左右（不超过30720字节）

## 测试步骤

### 步骤 1：准备串口监视器

1. 在 Arduino IDE 中，打开「工具 → 串口监视器」
2. 设置波特率为 **9600**
3. 应该看到启动信息（如果有的话）

### 步骤 2：测试手动写入（优先测试）

**这个最简单，先测试这个！**

1. 打开微信小程序，连接蓝牙
2. 点击「进入测高配置模式」
3. 等待约1.5秒
4. 在「危险高度」输入框输入 `16.8`
5. 点击「写入危险高度」
6. **查看串口监视器**

**预期看到：**
```
[DA16.8->[mm=168]OK A]
```

**如果看到：**
```
[DA6.8->[mm=68]OK A]
```
说明BLE传输时丢失了首位的 `1`！

**如果什么都没看到：**
- 固件没收到命令，或
- 配置模式未激活，或
- 命令被拒绝（格式检查失败）

### 步骤 3：测试自动标定

1. 在小程序中退出配置模式
2. 点击「F3 自动校准」
3. 按照向导完成标定步骤
4. **查看串口监视器和小程序Console**

**预期看到（串口）：**
```
[TB24.5->[mm=245]OK B]
[DA16.8->[mm=168]OK A]
```

如果只看到第一条，说明danger命令没发送或发送失败。

### 步骤 4：收集日志

**请将以下信息复制粘贴发给我：**

```
============ 手动写入 16.8 cm ============
小程序 Console 日志（筛选 [_f3SubmitHeightMm], [_sendF3HeightBleCmd], [高度命令], [回读校验]）：
[粘贴在这里]

固件串口日志（所有 [ ] 括号内容）：
[粘贴在这里]

最终显示的值：
危险高度: ___ cm
检测高度: ___ cm

============ 自动标定 ============
小程序 Console 日志（筛选 [自动标定], [_f3SubmitHeightMm], [_sendF3HeightBleCmd]）：
[粘贴在这里]

固件串口日志（所有 [ ] 括号内容）：
[粘贴在这里]

标定结果：
安全高度: ___ cm
危险高度: ___ cm
```

## 可能的问题原因分析

### 如果固件显示 `[DA6.8->[mm=68]OK A]`

✅ **找到问题**：BLE传输时丢失了 `1`，固件实际收到 `DA6.8` 而不是 `DA16.8`

可能原因：
1. **BLE发送缓冲区问题**：小程序的 `sendData()` 函数有问题
2. **HC-08接收问题**：蓝牙模块在接收长命令时丢数据
3. **固件接收缓冲区问题**：`rxBuf` 被提前截断

解决方案：
- 检查小程序的 `sendData()` 实现
- 在发送前增加延时
- 分段发送命令

### 如果固件什么都没显示

说明固件根本没进入 `f3TryShortHeightCmd` 或命令被拒绝。

可能原因：
1. **配置模式未激活**：`M1#` 命令失败
2. **命令格式检查失败**：`strchr(cmd, ':')` 或 `strchr(cmd, '|')` 检测到非法字符
3. **BLE根本没收到**：`pollBleSerial()` 没读到数据

解决方案：
- 检查小程序是否成功进入配置模式
- 检查 `M1#` 命令是否发送成功
- 检查蓝牙连接是否稳定

### 如果自动标定只有 `OK B` 没有 `OK A`

说明小程序没有发送danger命令，或发送失败。

查看小程序日志：
- 如果没有 `[自动标定] 步骤2: 写入danger`，说明代码逻辑问题
- 如果有但 `_f3SubmitHeightMm返回: false`，说明前置检查失败
- 如果有 `实际发送命令: DA...#` 但固件没收到，说明BLE传输问题

## 编译失败时的应急方案

如果精简版还是超出限制，可以：

1. **临时关闭其他功能**：
   - 改 `#define F3_SENSOR_SERIAL 0` 确保为0
   - 或者临时禁用某些不常用的命令

2. **只测试关键问题**：
   - 先用小程序日志判断是否发送
   - 用固件回读判断是否收到

3. **手动添加测试日志**：
   - 在关键位置闪烁LED
   - 用绿灯闪烁次数表示收到的mm值百位数

请先尝试烧录，如果还是超限，告诉我编译后的大小，我再进一步精简！

