# 终极BLE传输修复方案

## 问题根源

您的HC-08蓝牙模块在高速传输时**严重损坏数据**：

### 实际传输情况
| 发送 | 接收 | 结果 |
|------|------|------|
| `DA4.9#` | `D4.1` | A丢失，9→1 |
| `TB5#` | `TB`, `TA5`, `k` | 完全错乱 |

这是**硬件问题**，但我已经实现了软件层的终极解决方案。

## 新方案：逐字符发送

### 工作原理

**不再一次性发送完整命令**，而是：

1. **清空缓冲区**：先发送 `\r\n\r\n` 清空BLE接收缓冲区
2. **延迟启动**：等待2秒让BLE稳定
3. **逐字符发送**：每个字符间隔250ms发送
4. **完整日志**：每个字符都有独立日志

### 发送示例

发送 `DA4.9#` (5个字符)：
```
等待2000ms → 清空缓冲 → 等待500ms →
发送 'D' (68) → 等待250ms →
发送 'A' (65) → 等待250ms →
发送 '4' (52) → 等待250ms →
发送 '.' (46) → 等待250ms →
发送 '9' (57) → 等待250ms →
发送 '#' (35)
```

**总耗时**：约 2500ms + (5 × 250ms) = **3750ms**

### 时间调整

- **发送锁定时间**：2.8秒 → **12秒**
- **两次写入间隔**：4秒 → **15秒**
- **pending超时**：8秒 → **15秒**

## 测试步骤

1. **重新编译小程序**
2. **连接蓝牙**
3. **运行自动标定**
4. **观察小程序Console**

### 预期日志

```
[_sendF3HeightBleCmd] 开始逐字符发送: "DA4.9#"
[_sendF3HeightBleCmd] 发送字符 1/5: 'D' (68)
[_sendF3HeightBleCmd] 发送字符 2/5: 'A' (65)
[_sendF3HeightBleCmd] 发送字符 3/5: '4' (52)
[_sendF3HeightBleCmd] 发送字符 4/5: '.' (46)
[_sendF3HeightBleCmd] 发送字符 5/5: '9' (57)
[_sendF3HeightBleCmd] 发送字符 6/6: '#' (35)
```

### 固件日志（期望）

```
[F3] BLE RX DA4.9
[DA4.9->[mm=49] OK A]
```

或至少看到完整的命令字符串。

## 如果还是失败

如果逐字符发送还是失败，那就是**HC-08模块完全损坏**，必须：

### 硬件解决方案

1. **更换HC-08模块**（推荐）
2. **降低波特率到9600**：
   ```
   AT+BAUD4  // 设置为9600
   ```
   然后修改固件：
   ```cpp
   mySerial.begin(9600);  // 从115200改为9600
   ```

3. **检查接线**：
   - VCC → 5V
   - GND → GND  
   - TX → Pin 6 (Arduino)
   - RX → Pin 7 (Arduino)
   - 确保RX/TX接对了

4. **增加去耦电容**：
   - 在HC-08的VCC和GND之间接一个100nF陶瓷电容

## 临时手动方案

如果自动标定完全无法工作，可以：

1. **只用自动标定获取数值**（不写入）
2. **手动进入高级设置**
3. **逐个写入并反复重试**
4. **每次只写一个高度**
5. **写入后等待10秒再写下一个**

## 成功指标

### danger height (危险高度)
- 小程序回读：`parsed.dga=49`
- 固件日志：`[DA4.9->[mm=49] OK A]`
- UI显示：`4.9 cm`

### base height (检测高度)
- 小程序回读：`parsed.dgb=50`
- 固件日志：`[TB5->[mm=50] OK B]`
- UI显示：`5 cm`

## 代码修改总结

### 主要修改：`_sendF3HeightBleCmd`

- ✅ 清空BLE缓冲区
- ✅ 延迟2秒启动
- ✅ 逐字符发送（250ms间隔）
- ✅ 详细日志每个字符
- ✅ 锁定时间延长到12秒

### 次要修改：`_f3CalCommitHeightWrites`

- ✅ 两次写入间隔延长到15秒
- ✅ 保持danger先写，base后写

这是我能做的**最极限的软件优化**。如果这还不行，就必须更换硬件了。
