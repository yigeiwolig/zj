/* ================= 开发中弹窗（复用 my 页风格） ================= */
.custom-dialog-mask {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0,0,0,0.6);
  backdrop-filter: blur(5px);
  z-index: 99999;
  display: flex;
  align-items: center;
  justify-content: center;
}

.custom-dialog-box {
  width: 600rpx;
  background: #fff;
  border-radius: 40rpx;
  padding: 50rpx 40rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  box-shadow: 0 20rpx 60rpx rgba(0,0,0,0.2);
  animation: devPopIn 0.2s ease-out;
}

@keyframes devPopIn {
  from { transform: scale(0.9); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}

.cd-title { font-size: 34rpx; font-weight: 800; margin-bottom: 20rpx; color: #000; }
.cd-content { font-size: 28rpx; color: #666; margin-bottom: 50rpx; line-height: 1.5; }

.cd-btn-row { width: 100%; display: flex; gap: 30rpx; }
.cd-btn { flex: 1; height: 88rpx; line-height: 88rpx; border-radius: 44rpx; font-size: 30rpx; font-weight: 600; }
.cd-btn.confirm { background: #000; color: #fff; }
.cd-btn:active { opacity: 0.8; transform: scale(0.98); }

/* ================= 自动消失提示（无按钮，2秒后自动消失） ================= */
.auto-toast-mask {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0);
  z-index: 99999;
  display: flex;
  align-items: center;
  justify-content: center;
  pointer-events: none;
}

.auto-toast-box {
  background: rgba(0, 0, 0, 0.75);
  backdrop-filter: blur(20rpx);
  border-radius: 24rpx;
  padding: 24rpx 40rpx;
  max-width: 600rpx;
  text-align: center;
  animation: toastFadeIn 0.3s ease-out;
  pointer-events: auto;
}

@keyframes toastFadeIn {
  from {
    opacity: 0;
    transform: translateY(-20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.at-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #FFFFFF;
  margin-bottom: 8rpx;
}

.at-content {
  font-size: 26rpx;
  color: #FFFFFF;
  opacity: 0.9;
  line-height: 1.5;
}

/* ================= 自定义加载动画 ================= */
.loading-mask {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(12px);
  z-index: 99999;
  display: flex;
  align-items: center;
  justify-content: center;
}

.loader {
  width: 600rpx;
  height: 260rpx;
  border-radius: 20rpx;
  background: #fff;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-evenly;
  padding: 40rpx;
  box-shadow: 0 10rpx 30rpx rgba(0,0,0,0.12);
  box-sizing: border-box;
}

.loading-bar {
  width: 100%;
  height: 20rpx;
  background: #E5E5EA;
  border-radius: 20rpx;
  position: relative;
  overflow: hidden;
}

.loading-bar::after {
  content: "";
  position: absolute;
  left: 0;
  top: 0;
  height: 100%;
  width: 30%;
  background: linear-gradient(90deg, #007AFF, #5AC8FA);
  border-radius: 20rpx;
  animation: loadingMove 1.5s infinite;
}

@keyframes loadingMove {
  0% { left: -30%; }
  100% { left: 100%; }
}

.loader-label {
  font-size: 28rpx;
  color: #1D1D1F;
  font-weight: 500;
}

/* 主题变量 (包含领奖台动态色) */
.theme-gas {
  --bg-color: #000000;
  --text-main: #ffffff;
  --text-sub: #888888;
  --card-bg: #1f2937;
  --accent-color: #dc2626;
  --accent-shadow: rgba(220, 38, 38, 0.4);
  --glass-bg: rgba(30, 30, 30, 0.85);
  --glass-border: rgba(255, 255, 255, 0.15);
  --list-item-bg: rgba(255, 255, 255, 0.05);
  --spotlight: radial-gradient(circle at 50% 0%, #1f1f1f 0%, #000000 70%);
  --tab-bg: rgba(0,0,0,0.8);
  --podium-bg: #2a2a2a;
  --podium-gradient-from: rgba(0,0,0,0);
  --podium-gradient-to: #000000;
  --podium-text: #ffffff;
}

.theme-ev {
  --bg-color: #f3f4f6;
  --text-main: #111827;
  --text-sub: #6b7280;
  --card-bg: #ffffff;
  --accent-color: #2563eb;
  --accent-shadow: rgba(37, 99, 235, 0.4);
  --glass-bg: rgba(255, 255, 255, 0.85);
  --glass-border: rgba(0, 0, 0, 0.05);
  --list-item-bg: #ffffff;
  --spotlight: radial-gradient(circle at 50% 0%, #e0e7ff 0%, #f3f4f6 70%);
  --tab-bg: rgba(255,255,255,0.9);
  --podium-bg: #ffffff;
  --podium-gradient-from: rgba(255,255,255,0);
  --podium-gradient-to: #ffffff;
  --podium-text: #000000;
}

page { background-color: #000; color: #fff; box-sizing: border-box; font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', Helvetica, sans-serif; }
.container { width: 100%; height: 100vh; display: flex; flex-direction: column; position: relative; overflow: hidden; background-color: var(--bg-color); color: var(--text-main); transition: background 0.3s, color 0.3s; }
.spotlight-bg { position: absolute; top: 0; left: 0; width: 100%; height: 60%; background: var(--spotlight); z-index: 0; pointer-events: none; }
.text-accent { color: var(--accent-color); } .text-sub { color: var(--text-sub); }

/* 导航 */
.custom-nav-container { width: 100%; padding-bottom: 20rpx; z-index: 100; position: relative; }
.nav-bar-row { display: flex; align-items: center; justify-content: center; }
.brand-title { display: flex; flex-direction: column; justify-content: center; align-items: center; line-height: 1.2; }
.brand-sub { font-size: 18rpx; color: var(--text-sub); font-weight: bold; letter-spacing: 2px; margin-bottom: 4rpx; text-align: center; }
.brand-main { 
  font-size: 44rpx; 
  font-weight: 900; 
  font-style: italic; 
  color: var(--text-main);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 20rpx;
}

/* 顶部导航 */
.header {
  position: fixed; top: 0; left: 0; right: 0; height: 180rpx;
  padding-top: 80rpx; padding-left: 40rpx; padding-right: 40rpx;
  box-sizing: border-box; display: flex; justify-content: space-between; align-items: center;
  z-index: 10000; pointer-events: none;
  background: transparent; 
}
.back-icon {
  width: 70rpx;
  height: 70rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  pointer-events: auto;
  transition: all 0.2s;
  font-size: 52rpx;
  font-weight: 300;
  color: var(--text-main);
  line-height: 1;
}
.back-icon:active {
  transform: scale(0.95);
  opacity: 0.7;
}
.back-icon::before {
  display: none;
}

/* 🔴 左上角返回按钮 */
.back-btn {
  position: absolute;
  left: 32rpx;
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 101;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  transition: all 0.2s;
}

.back-btn:active {
  transform: scale(0.9);
  background: rgba(255, 255, 255, 0.2);
}

/* 管理员切换按钮 */
.admin-toggle-btn {
  display: inline-block;
  margin-left: 20rpx;
  font-size: 20rpx;
  padding: 4rpx 16rpx;
  border-radius: 20rpx;
  background: #F2F2F7;
  color: #8E8E93;
  border: 1rpx solid #E5E5EA;
  vertical-align: middle;
  transition: all 0.3s;
  font-weight: 600;
}

/* 开启状态的颜色 */
.admin-toggle-btn.on {
  background: #000;
  color: #fff;
  border-color: #000;
}
.filter-row { margin-top: 16rpx; display: flex; align-items: center; justify-content: center; }
.switch-capsule { display: flex; background: var(--glass-border); border: 1px solid var(--glass-border); border-radius: 12rpx; padding: 4rpx; transform: skewX(-10deg); backdrop-filter: blur(10px); }
.switch-item { padding: 10rpx 40rpx; font-size: 24rpx; font-weight: bold; color: var(--text-sub); transition: all 0.3s; border-radius: 8rpx; transform: skewX(10deg); }
.switch-item.active { background: var(--accent-color); color: #fff; box-shadow: 0 4rpx 12rpx var(--accent-shadow); }
.sort-row { display: flex; justify-content: center; gap: 40rpx; margin-top: 20rpx; padding-bottom: 10rpx; }
.sort-item { font-size: 22rpx; color: var(--text-sub); font-weight: bold; position: relative; transition: color 0.3s; }
.sort-item.active { color: var(--accent-color); }
.sort-item.active::after { content: ''; position: absolute; bottom: -6rpx; left: 50%; transform: translateX(-50%); width: 8rpx; height: 8rpx; border-radius: 50%; background: var(--accent-color); }

/* 内容 */
.content-area { flex: 1; position: relative; z-index: 10; overflow: hidden; }
.top-spacing { padding-top: 40rpx; }
.scroll-view { height: 100%; }

/* 领奖台 */
.podium-area { display: flex; justify-content: center; align-items: flex-end; padding: 20rpx 30rpx 60rpx; gap: 20rpx; }
.podium-item { display: flex; flex-direction: column; align-items: center; position: relative; overflow: visible !important; }
.podium-item.center { width: 38%; z-index: 20; } .podium-item.side { width: 29%; z-index: 10; }
.rank-num { font-size: 60rpx; font-weight: 900; font-style: italic; line-height: 0.8; margin-bottom: -10rpx; z-index: 5; text-shadow: 0 4rpx 8rpx rgba(0,0,0,0.1); margin-top: 10rpx; }
.crown-img { position: absolute; z-index: 99; animation: float 2s ease-in-out infinite; }
.center-crown { width: 80rpx; height: 80rpx; top: -70rpx; }
.side-crown { width: 60rpx; height: 60rpx; top: -50rpx; }
@keyframes float { 0% { transform: translateY(0); } 50% { transform: translateY(-6rpx); } 100% { transform: translateY(0); } }
.text-gold { color: #fbbf24; } .text-silver { color: #9ca3af; } .text-bronze { color: #d97706; }

.podium-card { width: 100%; aspect-ratio: 3/4; background: var(--card-bg); background-color: var(--podium-bg); border-top-left-radius: 16rpx; border-top-right-radius: 16rpx; overflow: hidden; position: relative; box-shadow: 0 10rpx 30rpx rgba(0,0,0,0.15); transition: background 0.3s; }
.podium-img { width: 100%; height: 100%; background-color: var(--podium-bg); display: block; }
.default-avatar { width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; background-color: var(--podium-bg); }
.icon-placeholder { font-size: 60rpx; opacity: 0.3; filter: grayscale(1); }
.gold-border { border-top: 6rpx solid #fbbf24; } .silver-border { border-top: 4rpx solid #9ca3af; } .bronze-border { border-top: 4rpx solid #b45309; }
.podium-info { position: absolute; bottom: 0; width: 100%; background: linear-gradient(to top, var(--podium-gradient-to), var(--podium-gradient-from)); padding: 20rpx 10rpx 10rpx; text-align: center; display: flex; flex-direction: column; }
.p-name { font-size: 22rpx; font-weight: bold; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; color: var(--podium-text); }
.p-name.big { font-size: 26rpx; text-transform: uppercase; }
.p-score { font-size: 32rpx; font-family: 'Impact', sans-serif; font-weight: bold; text-shadow: 0 2rpx 4rpx rgba(0,0,0,0.1); }
.p-score.big { font-size: 56rpx; line-height: 1; margin-top: 4rpx; color: #fbbf24; }
.p-dist { font-size: 20rpx; font-weight: bold; opacity: 0.9; margin-top: 2rpx; }
.unit { font-size: 24rpx; } .p-bike { font-size: 18rpx; color: #aaa; margin-top: 4rpx; text-transform: uppercase; }
.podium-base { width: 100%; height: 20rpx; border-bottom-left-radius: 8rpx; border-bottom-right-radius: 8rpx; opacity: 0.8; }
.base-gold { background: linear-gradient(90deg, #b45309, #fbbf24, #b45309); } .base-silver { background: linear-gradient(90deg, #4b5563, #9ca3af, #4b5563); } .base-bronze { background: linear-gradient(90deg, #78350f, #d97706, #78350f); }

/* 列表 */
.list-area { padding: 0 24rpx; }
.list-header { display: flex; padding: 20rpx 20rpx; font-size: 22rpx; color: var(--text-sub); border-bottom: 1px solid var(--glass-border); }
.lh-pos { width: 60rpx; } .lh-rider { flex: 1; } .lh-data { text-align: right; }
.list-item { display: flex; align-items: center; background: var(--list-item-bg); border: 1px solid var(--glass-border); margin-top: 16rpx; padding: 20rpx; border-radius: 12rpx; box-shadow: 0 2rpx 10rpx rgba(0,0,0,0.02); }
.li-rank { width: 60rpx; font-size: 32rpx; font-weight: bold; color: var(--text-sub); text-align: center; font-style: italic; }
.li-avatar { width: 80rpx; height: 80rpx; border-radius: 8rpx; background: #333; margin-right: 20rpx; }
.li-info { flex: 1; display: flex; flex-direction: column; }
.li-name { font-size: 28rpx; font-weight: bold; color: var(--text-main); }
.li-bike { font-size: 20rpx; color: var(--text-sub); text-transform: uppercase; margin-top: 4rpx; }
.li-data { text-align: right; display: flex; flex-direction: column; }
.li-angle { font-size: 36rpx; font-weight: bold; color: var(--text-main); font-family: 'Impact', sans-serif; }
.li-dist { font-size: 20rpx; color: var(--text-sub); }

/* 个人中心 */
.mine-header { display: flex; justify-content: space-between; align-items: center; padding: 0 40rpx 40rpx; }
.page-title { font-size: 48rpx; font-weight: 900; font-style: italic; color: var(--text-main); }
.identity-switch { display: flex; align-items: center; gap: 10rpx; padding: 10rpx 24rpx; border-radius: 30rpx; font-size: 22rpx; font-weight: bold; border: 1px solid var(--glass-border); background: var(--list-item-bg); color: var(--text-sub); transition: all 0.3s; }
.identity-switch.gas .dot { background: #dc2626; box-shadow: 0 0 10rpx #dc2626; } .identity-switch.ev .dot { background: #2563eb; box-shadow: 0 0 10rpx #2563eb; }
.dot { width: 12rpx; height: 12rpx; border-radius: 50%; }
.profile-card-v2 { display: flex; align-items: center; padding: 0 40rpx; margin-bottom: 50rpx; }
.avatar-box { position: relative; margin-right: 30rpx; }
.my-avatar { width: 160rpx; height: 160rpx; border-radius: 50%; border: 6rpx solid var(--card-bg); box-shadow: 0 10rpx 20rpx rgba(0,0,0,0.2); }
.edit-icon { position: absolute; bottom: 0; right: 0; width: 48rpx; height: 48rpx; background: var(--accent-color); border-radius: 50%; color: #fff; font-size: 24rpx; display: flex; justify-content: center; align-items: center; border: 4rpx solid var(--bg-color); }
.info-inputs { flex: 1; }
.name-input { font-size: 44rpx; font-weight: 900; color: var(--text-main); margin-bottom: 10rpx; border-bottom: 1px dashed transparent; }
.name-input:focus { border-bottom-color: var(--accent-color); }
.device-row { display: inline-flex; align-items: center; gap: 10rpx; font-size: 22rpx; color: var(--text-sub); background: var(--list-item-bg); padding: 6rpx 16rpx; border-radius: 8rpx; }
.status-dot.online { width: 10rpx; height: 10rpx; background: #22c55e; border-radius: 50%; }
.stats-grid { display: flex; justify-content: space-between; margin: 0 30rpx 50rpx; background: var(--card-bg); border-radius: 20rpx; padding: 30rpx 0; box-shadow: 0 10rpx 30rpx rgba(0,0,0,0.05); border: 1px solid var(--glass-border); }
.stat-item { flex: 1; display: flex; flex-direction: column; align-items: center; border-right: 1px solid var(--glass-border); }
.stat-item:last-child { border-right: none; }
.stat-val { font-size: 36rpx; font-weight: 900; color: var(--text-main); font-family: 'Impact', sans-serif; }
.stat-label { font-size: 20rpx; color: var(--text-sub); margin-top: 6rpx; text-transform: uppercase; }
.stat-item.highlight .stat-val { color: var(--accent-color); font-size: 44rpx; }
.bike-setting { margin: 0 30rpx 40rpx; background: var(--list-item-bg); padding: 24rpx 30rpx; border-radius: 16rpx; display: flex; align-items: center; justify-content: space-between; border: 1px solid var(--glass-border); }
.bike-setting .label { font-size: 24rpx; color: var(--text-sub); font-weight: bold; }
.bike-input { flex: 1; text-align: right; font-size: 28rpx; font-weight: bold; color: var(--text-main); margin: 0 20rpx; }
.edit-hint { font-size: 20rpx; color: var(--accent-color); opacity: 0.6; }
.history-section-v2 { padding: 0 30rpx; }
.history-list-v2 { display: flex; flex-direction: column; gap: 20rpx; }
.history-card { display: flex; align-items: center; justify-content: space-between; background: var(--card-bg); padding: 20rpx 30rpx; border-radius: 16rpx; border: 1px solid var(--glass-border); box-shadow: 0 4rpx 10rpx rgba(0,0,0,0.03); }
.h-left { display: flex; flex-direction: column; width: 120rpx; }
.h-angle { font-size: 40rpx; font-weight: 900; color: var(--accent-color); font-family: 'Impact', sans-serif; }
.h-angle .unit { font-size: 24rpx; color: var(--text-sub); }
.h-date { font-size: 20rpx; color: var(--text-sub); margin-top: 4rpx; }
.h-center { flex: 1; display: flex; flex-direction: column; align-items: center; padding: 0 30rpx; }
.h-bike { font-size: 22rpx; font-weight: bold; color: var(--text-main); margin-bottom: 6rpx; }
.h-line { width: 100%; height: 2rpx; background: var(--glass-border); }
.h-dist { font-size: 20rpx; color: var(--text-sub); margin-top: 6rpx; }
.h-right { width: 80rpx; height: 80rpx; border-radius: 12rpx; overflow: hidden; background: var(--list-item-bg); display: flex; align-items: center; justify-content: center; }
.h-thumb { width: 100%; height: 100%; }
.h-icon { font-size: 40rpx; }
.empty-tip { text-align: center; color: var(--text-sub); font-size: 24rpx; padding: 40rpx; }

/* Tab Bar */
.tab-bar { position: absolute; bottom: 0; width: 100%; height: 140rpx; display: flex; justify-content: space-around; align-items: flex-start; padding-top: 20rpx; z-index: 100; padding-bottom: env(safe-area-inset-bottom); background: var(--tab-bg); }
.glass-panel { background: var(--glass-bg); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); border: 1px solid var(--glass-border); }
.tab-item { display: flex; flex-direction: column; align-items: center; width: 120rpx; opacity: 0.4; transition: opacity 0.3s; color: var(--text-main); }
.tab-item.active { opacity: 1; color: var(--accent-color); }
.tab-icon-img { width: 48rpx; height: 48rpx; margin-bottom: 4rpx; }
.tab-text { font-size: 18rpx; font-weight: bold; letter-spacing: 1px; }
.upload-btn-wrapper { position: relative; top: -40rpx; }
.upload-btn { width: 110rpx; height: 110rpx; background: var(--accent-color); border-radius: 50%; display: flex; justify-content: center; align-items: center; box-shadow: 0 0 20rpx var(--accent-shadow); border: 6rpx solid var(--bg-color); transition: all 0.2s; }
.plus-icon { font-size: 60rpx; line-height: 1; margin-top: -8rpx; font-weight: 300; color: #fff; }

/* 弹窗 & 上传 */
.modal-mask { position: fixed; inset: 0; background: rgba(0,0,0,0.85); z-index: 999; display: flex; align-items: center; justify-content: center; backdrop-filter: blur(8px); }
.admin-modal { width: 600rpx; background: #1f2937; border: 1px solid rgba(255,255,255,0.1); border-radius: 24rpx; padding: 40rpx; position: relative; display: flex; flex-direction: column; }
.user-upload-modal { width: 650rpx; padding: 40rpx 30rpx; }
.edit-modal { max-height: 85vh; padding: 30rpx; }
.modal-title { font-size: 32rpx; font-weight: bold; color: #fff; margin-bottom: 30rpx; text-align: center; }
.modal-close { position: absolute; top: 20rpx; right: 30rpx; font-size: 50rpx; color: #888; padding: 20rpx; line-height: 0.5; z-index: 10; }
.media-row { display: flex; gap: 20rpx; margin-bottom: 30rpx; }
.upload-box { flex: 1; aspect-ratio: 1; background: rgba(255,255,255,0.03); border: 2rpx dashed var(--text-sub); border-radius: 16rpx; display: flex; justify-content: center; align-items: center; overflow: hidden; position: relative; transition: border-color 0.3s; }
.upload-placeholder { display: flex; flex-direction: column; align-items: center; color: #888; }
.plus-symbol { font-size: 80rpx; font-weight: 100; line-height: 1; margin-bottom: 10rpx; color: var(--accent-color); }
.uploaded-img { width: 100%; height: 100%; }
.input-group-user { margin-bottom: 30rpx; }
.modal-input { background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.1); height: 80rpx; border-radius: 12rpx; margin-bottom: 20rpx; padding: 0 20rpx; color: #fff; font-size: 28rpx; }
.row-inputs { display: flex; gap: 20rpx; } .half { flex: 1; }
.label { font-size: 22rpx; color: #aaa; margin: 10rpx 0; display: block; }

/* 蓝牙按钮美化 */
.bluetooth-section { background: rgba(0,0,0,0.3); border-radius: 16rpx; padding: 20rpx; border: 1px solid rgba(255,255,255,0.05); }
.bt-read-btn { position: relative; display: flex; align-items: center; justify-content: flex-start; padding: 0 40rpx; width: 100% !important; height: 140rpx; margin-bottom: 30rpx; border-radius: 24rpx; transition: all 0.3s ease; background: var(--card-bg); border: 2rpx solid var(--glass-border); box-shadow: 0 10rpx 20rpx rgba(0,0,0,0.1); color: var(--text-main); }
.bt-icon-wrap { width: 80rpx; height: 80rpx; background: var(--list-item-bg); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin-right: 30rpx; border: 1px solid var(--glass-border); }
.bt-svg { width: 40rpx; height: 40rpx; }
.bt-text-wrap { flex: 1; display: flex; flex-direction: column; align-items: flex-start; text-align: left; }
.bt-main-text { font-size: 30rpx; font-weight: bold; margin-bottom: 6rpx; }
.bt-sub-text { font-size: 20rpx; color: var(--text-sub); }
.status-light { width: 16rpx; height: 16rpx; border-radius: 50%; background: #666; box-shadow: inset 0 2rpx 4rpx rgba(0,0,0,0.5); transition: all 0.3s; }
.bt-read-btn.connected { border-color: var(--accent-color); background: linear-gradient(90deg, var(--card-bg), rgba(37, 99, 235, 0.05)); }
.bt-read-btn.connected .bt-icon-wrap { background: var(--accent-color); border-color: var(--accent-color); }
.bt-read-btn.connected .status-light.on { background: #22c55e; box-shadow: 0 0 15rpx #22c55e; }
.bt-read-btn.reading .bt-icon-wrap { animation: spin 1s linear infinite; }
@keyframes spin { 100% { transform: rotate(360deg); } }
.theme-ev .bt-read-btn { background: #ffffff; border: 1px solid #e5e7eb; }
.theme-ev .bt-icon-wrap { background: #f3f4f6; }

/* 数据屏 */
.data-display-screen { background: #000; border: 1px solid #333; border-radius: 12rpx; padding: 10rpx; font-family: 'Courier New', monospace; }
.data-row { display: flex; padding: 15rpx 0; } .border-top { border-top: 1px dashed #333; }
.data-cell { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; }
.data-cell .label { font-size: 20rpx; color: #666; margin-bottom: 6rpx; }
.data-cell .value { font-size: 32rpx; color: var(--accent-color); font-weight: bold; }
.data-cell .value.sm { font-size: 24rpx; color: #fff; }
.data-cell .unit { font-size: 20rpx; color: #888; }

/* 提交按钮 */
.save-btn.big-submit { width: 100% !important; height: 110rpx; margin-top: 40rpx; display: flex !important; align-items: center !important; justify-content: center !important; font-size: 34rpx; font-weight: 900; letter-spacing: 4rpx; border-radius: 55rpx; background: var(--accent-color); color: #fff; box-shadow: 0 10rpx 30rpx var(--accent-shadow); border: none; }
.save-btn-text { text-align: center; width: 100%; display: block; line-height: 110rpx; }

/* 管理员头像框 */
.admin-avatar-box { width: 200rpx; height: 200rpx; margin: 0 auto 30rpx; background: rgba(0,0,0,0.2); border: 4rpx solid var(--accent-color); border-radius: 20rpx; display: flex; justify-content: center; align-items: center; overflow: hidden; position: relative; }
.upload-placeholder-admin { display: flex; flex-direction: column; align-items: center; color: #fff; }
.plus-symbol-admin { font-size: 80rpx; font-weight: bold; line-height: 0.8; margin-bottom: 10rpx; }
.modal-footer { display: flex; gap: 20rpx; margin-top: 20rpx; }
.modal-btn { flex: 1; background: var(--accent-color); color: #fff; border-radius: 12rpx; height: 80rpx; line-height: 80rpx; font-weight: bold; font-size: 28rpx; }
.modal-btn.cancel { background: transparent; border: 1px solid #666; color: #ccc; }
.save-btn { margin-top: 30rpx; width: 100%; }
.input-group { margin-bottom: 20rpx; }
.radio-row { display: flex; gap: 20rpx; margin-bottom: 20rpx; }
.radio-btn { flex: 1; text-align: center; padding: 15rpx; background: rgba(255,255,255,0.05); color: #888; border-radius: 8rpx; font-size: 24rpx; font-weight: bold; border: 1px solid transparent; }
.radio-btn.active { background: var(--accent-color); color: #fff; border-color: rgba(255,255,255,0.2); }
.admin-tag { font-size: 18rpx; background: #dc2626; color: white; padding: 2rpx 8rpx; border-radius: 4rpx; margin-left: 10rpx; vertical-align: middle; }
.admin-bar { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20rpx; }
.admin-title { font-size: 28rpx; font-weight: bold; color: var(--text-main); }
.add-btn { background: var(--accent-color); color: white; padding: 10rpx 30rpx; border-radius: 30rpx; font-size: 24rpx; font-weight: bold; box-shadow: 0 4rpx 10rpx var(--accent-shadow); }
.admin-item { padding: 15rpx 20rpx; }
.highlight { color: #fbbf24; }
.score-tag { font-size: 18rpx; background: #333; padding: 2rpx 6rpx; border-radius: 4rpx; color: #aaa; margin-left: 10rpx; font-weight: normal; }
.action-btns { display: flex; gap: 10rpx; }
.btn-edit, .btn-del { font-size: 20rpx; padding: 8rpx 16rpx; border-radius: 8rpx; font-weight: bold; }
.btn-edit { background: rgba(255,255,255,0.1); color: var(--text-main); border: 1px solid var(--glass-border); }
.btn-del { background: rgba(220, 38, 38, 0.2); color: #dc2626; border: 1px solid rgba(220, 38, 38, 0.3); }

/* 交互反馈 */
.btn-hover { transform: scale(0.92); opacity: 0.7; transition: all 0.1s ease-in-out; }
.switch-item.btn-hover { transform: scale(0.98); background-color: rgba(255,255,255,0.2); }
.modal-close.btn-hover { transform: rotate(90deg); color: #fff; }
.upload-box.btn-hover { border-color: var(--accent-color); background-color: rgba(255,255,255,0.05); transform: scale(0.98); }
.upload-btn-wrapper.btn-hover .upload-btn { transform: scale(0.9) rotate(90deg); background-color: #b91c1c; transition: all 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275); }