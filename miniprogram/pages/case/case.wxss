/* ================= 本页自定义加载动画（白色虚化 + 你的 loader，同 my 页） ================= */
.loading-mask {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(12px);
  z-index: 99999;
  display: flex;
  align-items: center;
  justify-content: center;
}

.loader {
  width: 600rpx;
  height: 260rpx;
  border-radius: 20rpx;
  background: #fff;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-evenly;
  padding: 40rpx;
  box-shadow: 0 10rpx 30rpx rgba(0,0,0,0.12);
  box-sizing: border-box;
}

.loading-bar {
  width: 100%;
  height: 20rpx;
  background: #E5E5EA;
  border-radius: 20rpx;
  position: relative;
  overflow: hidden;
}

.loading-bar::after {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 50%;
  height: 100%;
  background: #1c1c1e;
  border-radius: 20rpx;
  z-index: 1;
  animation: loadingBar 0.6s alternate infinite ease-in-out;
}

.loader-label {
  color: #1c1c1e;
  font-size: 28rpx;
  font-weight: 700;
  animation: labelBlink 0.6s alternate infinite;
}

@keyframes labelBlink {
  from { opacity: 0.35; }
  to   { opacity: 1; }
}

@keyframes loadingBar {
  0%   { left: 0%; }
  50%  { left: 25%; }
  100% { left: 50%; }
}

/* 全局样式 */
page {
  background-color: #F2F2F7;
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', Helvetica, Segoe UI, Arial, Roboto, 'PingFang SC', 'miui', 'Hiragino Sans GB', 'Microsoft Yahei', sans-serif;
  box-sizing: border-box;
  /* 🔴 核心修复：禁用整个页面滚动，禁止回弹 */
  overflow: hidden;
  height: 100vh;
}

/* 0. 背景大水印 */
.brand-bg-watermark {
  position: fixed;
  top: 45%;
  left: 50%;
  transform: translate(-50%, -50%) rotate(-30deg);
  font-size: 100rpx;
  font-weight: 900;
  color: rgba(0, 0, 0, 0.03);
  pointer-events: none;
  z-index: -1;
  white-space: nowrap;
}

/* 1. 顶部大标题 */
.custom-header {
  position: sticky;
  top: 0;
  z-index: 50;
  background-color: rgba(242, 242, 247, 0.95);
  backdrop-filter: blur(20px);
  padding-bottom: 10px;
}

.header-content {
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  padding: 0 32rpx;
}

/* 顶部导航 */
.header {
  position: fixed; top: 0; left: 0; right: 0; height: 180rpx;
  padding-top: calc(env(safe-area-inset-top, 0px) + 88rpx); 
  padding-left: 40rpx; 
  padding-right: 40rpx;
  box-sizing: border-box; 
  display: flex; 
  justify-content: space-between; 
  align-items: center;
  z-index: 10000; /* 🔴 提高 z-index 确保返回键显示 */
  pointer-events: none;
  background: transparent; 
}

/* 🔴 统一返回键样式 - 全局标准 */
.back-icon {
  position: absolute;
  left: 40rpx;
  top: calc(env(safe-area-inset-top, 0px) + 88rpx);
  width: 70rpx;
  height: 70rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  pointer-events: auto;
  transition: all 0.2s;
  z-index: 10001;
}
.back-icon:active {
  transform: scale(0.95);
  opacity: 0.7;
  background-color: rgba(0, 0, 0, 0.05);
}
.back-icon::before {
  display: none;
}

/* 统一返回箭头样式 */
.back-arrow {
  font-size: 48rpx;
  font-weight: 300;
  color: #000;
  line-height: 1;
}

.header-title {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 20rpx;
}

/* 管理员切换按钮 */
.admin-toggle-btn {
  display: inline-block;
  margin-left: 20rpx;
  font-size: 20rpx;
  padding: 4rpx 16rpx;
  border-radius: 20rpx;
  background: #F2F2F7;
  color: #8E8E93;
  border: 1rpx solid #E5E5EA;
  vertical-align: middle;
  transition: all 0.3s;
  font-weight: 600;
}

/* 开启状态的颜色 */
.admin-toggle-btn.on {
  background: #000;
  color: #fff;
  border-color: #000;
}

/* 管理员子模式切换按钮 */
.admin-submode-toggle {
  display: flex;
  gap: 10rpx;
  margin-top: 20rpx;
  justify-content: center;
}

.submode-btn {
  padding: 8rpx 24rpx;
  font-size: 24rpx;
  border-radius: 20rpx;
  background: #F2F2F7;
  color: #8E8E93;
  border: 1rpx solid #E5E5EA;
  transition: all 0.3s;
  font-weight: 500;
}

.submode-btn.active {
  background: #007AFF;
  color: #fff;
  border-color: #007AFF;
}

.header-title {
  font-size: 36rpx;
  font-weight: 800;
  color: #000;
  letter-spacing: 1px;
}

.debug-btn {
  position: absolute;
  right: 120rpx;
  padding: 8rpx 20rpx;
  background: #FF3B30;
  color: #fff;
  font-size: 24rpx;
  border-radius: 20rpx;
  font-weight: 600;
}

/* 1. 修复 Tab 被遮挡的问题 */
.nav-container {
  position: fixed;
  left: 0;
  width: 100%;
  z-index: 49;
  background-color: #F2F2F7;
  /* 🔴 修改：增加顶部内边距，把 Tab 往下挤一点，就不会被标题栏挡住了 */
  padding-top: 20rpx; 
  padding-bottom: 10rpx;
}

/* Tab 滚动区域 */
.scroll-wrapper {
  width: 100%;
  white-space: nowrap;
  position: relative;
  z-index: 10; /* 保证 Tab 在搜索条上面 */
}

.tab-list {
  display: flex;
  padding: 0 32rpx;
  position: relative; /* 为滑块定位 */
  height: 80rpx;
  align-items: center;
}

/* 🆕 黑色滑块 */
.tab-slider {
  position: absolute;
  top: 10rpx;
  height: 60rpx;
  background: #000000;
  border-radius: 30rpx;
  z-index: 1; 
  transition: left 0.3s cubic-bezier(0.25, 1, 0.5, 1), 
              width 0.3s cubic-bezier(0.25, 1, 0.5, 1);
  /* 关键：给个下阴影遮丑 */
  box-shadow: 0 4rpx 0 #000;
}

/* 文字按钮 */
.tab-item {
  display: inline-block;
  padding: 16rpx 40rpx;
  font-size: 28rpx;
  font-weight: 500;
  color: #8E8E93;
  margin-right: 20rpx;
  position: relative;
  z-index: 2;
  transition: color 0.3s;
  
  /* 🔴 核心修复：强制文字不换行，保证宽度计算准确 */
  white-space: nowrap; 
}

.tab-item.active {
  color: #FFFFFF; /* 选中变白 */
  font-weight: 600;
}

/* 🆕 列表飞入动画 */
@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(40rpx); }
  to { opacity: 1; transform: translateY(0); }
}

.move-animation {
  /* 每次渲染都触发简单的上浮动画 */
  animation: fadeInUp 0.5s ease-out forwards;
}

/* 🔴 核心修改：定义 scroll-view 样式 */
.main-scroll-view {
  width: 100%;
  position: fixed;
  left: 0;
  z-index: 1;
  background-color: #F2F2F7;
}

/* 2. 把列表内容往下推，解决顶部被遮挡的问题 */
/* 列表容器 */
.video-list {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  padding: 20rpx 32rpx;
  position: relative;
  z-index: 1;
  /* 默认状态 (搜索框收起时)：Nav高(80) + 间距 */
  padding-top: 20rpx; 
  /* 关键：增加过渡动画，让列表滑上滑下 */
  transition: padding-top 0.3s cubic-bezier(0.25, 1, 0.5, 1);
}

/* 🔴 核心修复：当搜索框出来时，列表自动往下移 */
.video-list.pushed-down {
  /* 展开状态：默认(20) + 搜索框高(120) */
  padding-top: 140rpx; 
}

.ios-card {
  width: 48%;
  background: #FFFFFF;
  border-radius: 24rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.03);
  margin-bottom: 24rpx;
  display: flex;
  flex-direction: column;
}

.thumb-box {
  width: 100%;
  height: 0;
  padding-bottom: 133%;
  background: #eee;
  position: relative;
  overflow: hidden;
}

/* 🆕 纯白高亮水印 */
.card-watermark-overlay {
  position: absolute;
  top: 50%; left: 50%;
  transform: translate(-50%, -50%) rotate(-30deg);
  font-size: 44rpx;
  font-weight: 900;
  /* 纯白 */
  color: rgba(255, 255, 255, 0.9); 
  border: 4rpx solid rgba(255, 255, 255, 0.9);
  padding: 8rpx 24rpx;
  border-radius: 12rpx;
  pointer-events: none;
  z-index: 5;
  text-shadow: 0 4rpx 10rpx rgba(0,0,0,0.5); /* 加阴影更清晰 */
}

.card-content {
  padding: 20rpx;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

/* 3. 修复字体太小的问题 */
.card-title {
  /* 🔴 修改：加大字号 */
  font-size: 34rpx; 
  font-weight: 700;
  color: #000;
  line-height: 1.4;
  margin-bottom: 16rpx;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.card-meta {
  /* 🔴 修改：加大字号 */
  font-size: 26rpx; 
  font-weight: 500;
  color: #666;
  background: #F5F5F7;
  padding: 8rpx 16rpx;
  border-radius: 8rpx;
  align-self: flex-start;
}

/* 4. 底部录制按钮 */
/* 底部按钮容器 */
.fab-center {
  position: fixed;
  bottom: 60rpx; /* 稍微抬高一点，防误触 */
  left: 50%;
  transform: translateX(-50%);
  z-index: 90;
  width: 130rpx;
  height: 130rpx;
}

/* 🆕 新的发布按钮 (黑底，更像上传/发布) */
.publish-btn {
  width: 130rpx !important;
  height: 130rpx !important;
  padding: 0 !important;
  margin: 0 !important;
  border-radius: 50% !important;
  
  /* 🔴 改为黑色背景，显高级，代表"创作/发布" */
  background: #000000; 
  
  display: flex;
  align-items: center;
  justify-content: center;
  
  /* 增加投影，让它浮起来 */
  box-shadow: 0 10rpx 30rpx rgba(0, 0, 0, 0.3);
  border: 4rpx solid #ffffff; /* 加个白边，更有层次感 */
}

.publish-btn:active {
  transform: scale(0.95); /* 点击时的按压效果 */
  opacity: 0.9;
  /* 🔴 优化：使用 will-change 提升动画性能 */
  will-change: transform;
}

/* 🆕 纯 CSS 绘制的白色加号 (+) */
.icon-plus {
  position: relative;
  width: 50rpx;
  height: 50rpx;
}

.icon-plus::before,
.icon-plus::after {
  content: '';
  position: absolute;
  background-color: #ffffff; /* 白色加号 */
  border-radius: 4rpx;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

/* 横线 */
.icon-plus::before {
  width: 50rpx;
  height: 6rpx;
}

/* 竖线 */
.icon-plus::after {
  width: 6rpx;
  height: 50rpx;
}

/* 5. 弹窗系统 */
.modal-overlay {
  position: fixed;
  top: 0; left: 0; width: 100%; height: 100%;
  background: rgba(0, 0, 0, 0.4);
  backdrop-filter: blur(8px);
  z-index: 2000;
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  visibility: hidden;
  transition: opacity 0.3s;
}

.modal-overlay.active {
  opacity: 1;
  visibility: visible;
}

.ios-dialog {
  position: relative; 
  background: #FFFFFF;
  width: 600rpx;
  border-radius: 48rpx;
  padding: 50rpx 40rpx;
  text-align: center;
  transform: scale(0.95);
  transition: transform 0.3s;
}

/* 🔴 新增：抖动动画 */
@keyframes shake {
  0%, 100% { transform: translateX(0) scale(0.95); }
  10%, 30%, 50%, 70%, 90% { transform: translateX(-10rpx) scale(0.95); }
  20%, 40%, 60%, 80% { transform: translateX(10rpx) scale(0.95); }
}

.ios-dialog.shake {
  animation: shake 0.3s ease-in-out;
}

.modal-overlay.active .ios-dialog {
  transform: scale(1);
}

.left-align { text-align: left; }
.center { text-align: center; }

.emoji-icon { font-size: 80rpx; margin-bottom: 20rpx; }
.dialog-title { font-size: 36rpx; font-weight: 700; margin-bottom: 30rpx; color: #000; }
.dialog-desc { font-size: 28rpx; color: #666; line-height: 1.6; margin-bottom: 40rpx; }
.highlight { color: #007AFF; font-weight: 700; }

.form-group { 
  margin-bottom: 40rpx; 
}
.form-label { display: block; font-size: 26rpx; font-weight: 600; color: #333; margin-bottom: 16rpx; }

.ios-input {
  width: 100%;
  height: 88rpx;
  background: #F5F5F7;
  border-radius: 24rpx;
  padding: 0 24rpx;
  font-size: 30rpx;
  box-sizing: border-box;
}

.ios-select {
  width: 100%;
  height: 88rpx;
  line-height: 88rpx;
  background: #F5F5F7;
  border-radius: 24rpx;
  padding: 0 24rpx;
  font-size: 30rpx;
  color: #000;
  box-sizing: border-box;
}

/* 🔴 新增：iOS 风格自定义选择器样式（用于模拟器） */
.custom-picker-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.4);
  backdrop-filter: blur(8rpx);
  z-index: 10001; /* 🔴 确保在 modal-overlay (2000) 之上 */
  display: flex;
  align-items: flex-end;
  animation: fadeIn 0.3s ease-out;
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes slideUp {
  from {
    transform: translateY(100%);
  }
  to {
    transform: translateY(0);
  }
}

.custom-picker-content {
  width: 100%;
  background: #F2F2F7;
  border-radius: 40rpx 40rpx 0 0;
  padding-bottom: env(safe-area-inset-bottom);
  max-height: 60vh;
  display: flex;
  flex-direction: column;
  animation: slideUp 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 -8rpx 32rpx rgba(0, 0, 0, 0.1);
}

.picker-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx 32rpx;
  background: #F2F2F7;
  border-bottom: 1rpx solid rgba(0, 0, 0, 0.05);
  position: relative;
}

.picker-header::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 1rpx;
  background: linear-gradient(to right, transparent, rgba(0, 0, 0, 0.1), transparent);
}

.picker-cancel,
.picker-confirm {
  font-size: 34rpx;
  color: #007AFF;
  padding: 12rpx 24rpx;
  font-weight: 500;
  border-radius: 12rpx;
  transition: background 0.2s;
}

.picker-cancel:active,
.picker-confirm:active {
  background: rgba(0, 122, 255, 0.1);
}

.picker-title {
  font-size: 36rpx;
  font-weight: 600;
  color: #000;
  letter-spacing: -0.5rpx;
}

.picker-view {
  height: 360rpx;
  width: 100%;
  background: #fff;
  position: relative;
}

/* 顶部和底部渐变遮罩 */
.picker-view::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 120rpx;
  background: linear-gradient(to bottom, rgba(255, 255, 255, 0.95), rgba(255, 255, 255, 0));
  pointer-events: none;
  z-index: 2;
}

.picker-view::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 120rpx;
  background: linear-gradient(to top, rgba(255, 255, 255, 0.95), rgba(255, 255, 255, 0));
  pointer-events: none;
  z-index: 2;
}

.picker-item {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 72rpx;
  font-size: 34rpx;
  color: #000;
  font-weight: 400;
  transition: all 0.2s;
  position: relative;
}

/* 选中项指示器 - 使用额外的元素 */
.custom-picker-content::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 0;
  right: 0;
  height: 72rpx;
  transform: translateY(-50%);
  margin-top: 120rpx; /* 调整位置，避开 header */
  background: rgba(0, 122, 255, 0.06);
  border-top: 1rpx solid rgba(0, 122, 255, 0.2);
  border-bottom: 1rpx solid rgba(0, 122, 255, 0.2);
  pointer-events: none;
  z-index: 1;
}

.dialog-btn {
  background: #000;
  color: #fff;
  width: 100% !important;
  padding: 24rpx 0;
  border-radius: 24rpx;
  font-size: 32rpx;
  font-weight: 600;
  margin-top: 20rpx;
}

/* 成功页样式 */
.status-badge {
  display: inline-block;
  background: #E8F5E9;
  color: #2E7D32;
  padding: 8rpx 20rpx;
  border-radius: 12rpx;
  font-size: 24rpx;
  font-weight: 600;
  margin-bottom: 30rpx;
}
.user-name { font-size: 32rpx; font-weight: 700; margin-bottom: 10rpx; }

/* 6. 全屏相机 */
.camera-full-screen {
  position: fixed; 
  top: 0; 
  left: 0; 
  width: 100vw; 
  height: 100vh; 
  z-index: 3000; 
  background: #000;
  /* 🔴 禁用页面滑动 */
  touch-action: none;
  overflow: hidden;
  /* 🔴 优化：使用 GPU 加速，提升动画性能 */
  will-change: transform, opacity, border-radius;
  transform: translateZ(0);
  
  /* 🆕 关键：transform-origin 设置在底部中心（按钮位置） */
  /* 按钮位置：bottom: 60rpx, 按钮高度130rpx，中心在底部往上 125rpx */
  /* 使用 center bottom 让缩放从底部中心开始 */
  transform-origin: center bottom;
  transform: scale(0.05); /* 🆕 初始很小，从按钮大小开始 */
  opacity: 0;
  border-radius: 50%; /* 🆕 初始圆形，模拟从圆形按钮弹出 */
  /* 🔴 优化：使用更高效的动画属性，减少重绘 */
  /* 🔴 优化：缩短退场动画时间，提升响应速度 */
  transition: transform 0.25s cubic-bezier(0.4, 0, 0.2, 1),
              opacity 0.2s ease-out,
              border-radius 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

/* 🆕 动画进行中：保持初始状态（从按钮位置，很小，圆形） */
.camera-full-screen.animating {
  transform: scale(0.05);
  opacity: 0;
  border-radius: 50%;
}

/* 🆕 动画完成：从按钮位置直接放大到全屏，带弹性弹跳效果 */
.camera-full-screen.animated {
  transform: scale(1); /* 🆕 放大到全屏，从底部中心展开 */
  opacity: 1;
  border-radius: 0; /* 变成全屏矩形 */
}
.cam-overlay {
  position: absolute; top: 0; left: 0; width: 100%; height: 100%;
  pointer-events: none;
  display: flex; flex-direction: column; justify-content: center; align-items: center;
}
/* =========================== */
/* 🆕 录制页面 UI 升级 (高级版) */
/* =========================== */

/* 1. 顶部时间：纯白 + 红色呼吸点 */
.rec-header-layout {
  position: absolute;
  top: 120rpx;
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 16rpx; /* 点和字的间距 */
  opacity: 0.9;
}

/* 那个红点 */
.rec-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background-color: #FF3B30;
  opacity: 0; /* 默认隐藏 */
  transition: opacity 0.15s; /* 🔴 优化：缩短到 0.15s，与摄像头同步 */
}

/* 录制时，红点闪烁 */
.rec-dot.active {
  opacity: 1;
  animation: blinkDot 1s infinite;
}

@keyframes blinkDot {
  0% { opacity: 1; }
  50% { opacity: 0.3; }
  100% { opacity: 1; }
}

/* 时间文字：白色、高级字体 */
.rec-time-text {
  color: #FFFFFF; /* 🔴 改回白色，不显low */
  font-size: 48rpx; /* 不需要巨大，精致一点 */
  font-weight: 500;
  font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Text', Roboto, sans-serif;
  letter-spacing: 2rpx;
  text-shadow: 0 2rpx 6rpx rgba(0,0,0,0.3); /* 淡淡的投影保证清晰度 */
}

/* 2. 隐私提示：白色背景清晰可见 */
.privacy-tip {
  position: absolute;
  top: 30%; /* 稍微往上放一点，不遮挡视线 */
  left: 50%;
  transform: translate(-50%, -50%) scale(0.9);
  
  /* 🔴 核心：白色背景，清晰可见 */
  background: #FFFFFF; /* 纯白背景 */
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.15); /* 添加阴影增加层次感 */
  
  padding: 24rpx 40rpx;
  border-radius: 60rpx; /* 胶囊形状 */
  
  display: flex;
  align-items: center;
  justify-content: center;
  
  opacity: 0;
  transition: all 0.2s ease-out; /* 🔴 优化：从 0.6s 缩短到 0.2s，快速退场 */
  pointer-events: none;
}

.privacy-tip.show {
  opacity: 1;
  transform: translate(-50%, -50%) scale(1);
}

.privacy-text {
  color: #333333; /* 深色文字，在白色背景上清晰可见 */
  font-size: 28rpx;
  font-weight: 500; /* 稍微加粗，更清晰 */
  letter-spacing: 1rpx;
  text-align: center;
  line-height: 1.5;
}

.cam-controls {
  position: absolute; bottom: 0; left: 0; width: 100%; height: 240rpx;
  background: rgba(0,0,0,0.5);
  display: flex; align-items: center; justify-content: space-around;
  padding-bottom: 40rpx;
  z-index: 9999;
}
.cam-btn { color: #fff; font-size: 32rpx; }

/* 3. 按钮丝滑变形与呼吸动画 */

/* 外圈 (保持不动) */
.shutter-outer {
  width: 150rpx;
  height: 150rpx;
  border: 8rpx solid rgba(255, 255, 255, 1);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform 0.2s;
}
/* 点击时的按压感 */
.shutter-outer:active {
  transform: scale(0.95);
  opacity: 0.8;
}

/* 内核 (红色部分) */
.shutter-inner {
  width: 120rpx;  /* 初始圆大小 */
  height: 120rpx;
  background: #FF3B30;
  border-radius: 60rpx; /* 🔴 改为固定值，与宽高的一半一致，确保过渡平滑 */
  
  /* 🔴 核心：分别设置过渡，让圆角变化和尺寸变化同步进行 */
  /* 🔴 优化：缩短动画时间，退场时更快 */
  transition: width 0.2s cubic-bezier(0.4, 0, 0.2, 1),
              height 0.2s cubic-bezier(0.4, 0, 0.2, 1),
              border-radius 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

/* 录制状态：变成圆角方块 */
.shutter-inner.recording {
  width: 60rpx;   /* 变小 */
  height: 60rpx;
  border-radius: 12rpx; /* 变成圆角矩形 */
  background: #FF3B30;
}

/* 🆕 呼吸动画定义 */
@keyframes breathe {
  0% {
    transform: scale(1);
    opacity: 1;
  }
  50% {
    transform: scale(0.8); /* 缩小一点 */
    opacity: 0.7;
  }
  100% {
    transform: scale(1);
    opacity: 1;
  }
}

/* 当同时拥有 recording 和 breathing 类时触发动画 */
.shutter-inner.breathing {
  animation: breathe 1.5s infinite ease-in-out;
}

/* 🆕 管理员上传区域样式 */
.upload-area {
  width: 100%;
  height: 300rpx;
  background: #F2F2F7;
  border-radius: 24rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  margin-bottom: 20rpx;
  border: 2rpx dashed #C7C7CC;
  overflow: hidden;
  position: relative;
}

.upload-icon {
  font-size: 60rpx;
  color: #8E8E93;
  margin-bottom: 10rpx;
}

.preview-video {
  width: 100%;
  height: 100%;
}

.re-upload-text {
  text-align: center;
  font-size: 24rpx;
  color: #007AFF;
  margin-bottom: 30rpx;
  margin-top: -10rpx;
}

/* 🆕 列表卡片封面图 */
.card-cover-img {
  position: absolute; /* 🔴 必须加这行！否则高度是 0 */
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 24rpx; /* 加上圆角更好看 */
}

.play-icon-overlay {
  position: absolute;
  bottom: 10rpx;
  right: 10rpx;
  background: rgba(0,0,0,0.5);
  color: #fff;
  font-size: 20rpx;
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  backdrop-filter: blur(4px);
}

/* 🆕 编辑图标 */
.edit-icon-overlay {
  position: absolute;
  bottom: 10rpx; right: 10rpx;
  background: rgba(0,122,255, 0.8); /* 蓝色 */
  color: #fff;
  width: 44rpx; height: 44rpx; border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 24rpx;
}

/* 删除按钮 */
.delete-btn {
  position: absolute;
  top: 10rpx;
  left: 10rpx;
  width: 44rpx;
  height: 44rpx;
  background: rgba(255, 59, 48, 0.9);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10;
}
.delete-x {
  color: #fff;
  font-size: 32rpx;
  line-height: 1;
  font-weight: bold;
  margin-top: -4rpx;
}

/* =========================== */
/* 🆕 视频播放器弹窗样式 */
/* =========================== */
.video-player-modal {
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100%;
  z-index: 5000;
  visibility: hidden;
  opacity: 0;
  transition: all 0.3s;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

.video-player-modal.active {
  visibility: visible;
  opacity: 1;
}

.player-backdrop {
  position: absolute;
  top: 0; left: 0;
  width: 100%; height: 100%;
  background: rgba(0, 0, 0, 0.9);
}

.player-container {
  width: 100%;
  position: relative;
  z-index: 5001;
  display: flex;
  flex-direction: column;
}

.main-video {
  width: 100%;
  height: 450rpx;
  background: #000;
}

.video-info-box {
  background: #1c1c1e;
  padding: 30rpx;
  color: #fff;
  border-bottom-left-radius: 24rpx;
  border-bottom-right-radius: 24rpx;
}

.info-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 20rpx;
}

.info-title {
  font-size: 36rpx;
  font-weight: 700;
  color: #fff;
  line-height: 1.4;
  flex: 1;
  margin-right: 20rpx;
}

.info-close {
  width: 60rpx;
  height: 60rpx;
  background: rgba(255,255,255,0.2);
  border-radius: 50%;
  text-align: center;
  line-height: 55rpx;
  font-size: 40rpx;
  color: #fff;
}

.info-tags {
  display: flex;
  gap: 16rpx;
  margin-bottom: 20rpx;
}

.tag-pill {
  padding: 8rpx 20rpx;
  background: #0A84FF;
  border-radius: 8rpx;
  font-size: 24rpx;
  font-weight: 600;
}

.tag-pill.dark {
  background: #3a3a3c;
  color: #AEAEB2;
}

.info-desc {
  font-size: 26rpx;
  color: #8E8E93;
  line-height: 1.5;
}

/* 全屏播放器 */
.fullscreen-player-container {
  position: fixed;
  top: 0; left: 0;
  width: 100vw; height: 100vh;
  background: #000;
  z-index: 10001; /* 🔴 修复：确保在 header (10000) 之上 */
}
.fullscreen-video { width: 100%; height: 100%; }

/* 自动水印 */
.auto-watermark {
  position: absolute;
  top: 50%;
  left: 50%;
  /* 使用 translate(-50%, -50%) 它是核心，能让元素真正的中心点对齐屏幕中心 */
  transform: translate(-50%, -50%) rotate(-30deg);
  opacity: 0.3;
  pointer-events: none;
  z-index: 2000;
  white-space: nowrap;
}

/* 🔴 新增：视频保护层（用于加密防护） */
.video-protection-layer {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: transparent;
  pointer-events: none; /* 不阻止视频播放，但可以防止某些操作 */
  z-index: 1999;
}
.wm-text {
  font-size: 60rpx;
  font-weight: 900;
  color: #fff;
  text-shadow: 0 2px 10px rgba(0,0,0,0.5);
}

/* 关闭按钮 & 底部信息 */
.close-video-btn {
  position: absolute;
  top: 80rpx; /* 🔴 修复：下移，避免太高 */
  left: 30rpx;
  width: 70rpx;
  height: 70rpx;
  background: rgba(0,0,0,0.6);
  border-radius: 50%;
  color: #fff;
  text-align: center;
  line-height: 70rpx;
  font-size: 50rpx;
  z-index: 10002; /* 🔴 修复：确保在最上层 */
  backdrop-filter: blur(10rpx);
}
.video-bottom-info {
  position: absolute;
  bottom: 60rpx;
  left: 0;
  width: 100%;
  padding: 30rpx;
  background: linear-gradient(to top, rgba(0,0,0,0.8), transparent);
}
.v-title {
  color: #fff;
  font-size: 34rpx;
  font-weight: 700;
  margin-bottom: 20rpx;
  white-space: normal;
}
.v-tags { display: flex; flex-direction: row; }
.v-tag {
  padding: 6rpx 16rpx;
  border-radius: 8rpx;
  font-size: 24rpx;
  margin-right: 16rpx;
}
.v-tag.blue { background: #0A84FF; color: #fff; }
.v-tag.gray { background: rgba(255,255,255,0.3); color: #fff; }

/* 🆕 白色搜索栏 */
/* 搜索栏外壳 */
.search-notch {
  width: 100%;
  height: 0;
  overflow: hidden;
  transition: height 0.3s cubic-bezier(0.25, 1, 0.5, 1); /* 弹性动画 */
  
  position: absolute;
  top: 100%;
  left: 0;
  z-index: 5; 
  
  /* 🔴 稍微加大一点负边距，让连接更紧密 */
  margin-top: -34rpx; 
  padding-top: 34rpx; 
  
  display: flex;
  justify-content: center;
}

.search-notch.open {
  height: 120rpx;
}

/* 2. 修复搜索框看不见的问题 */
.search-inner {
  width: 92%;
  height: 80rpx;
  background: #FFFFFF; /* 纯白背景 */
  
  border-bottom-left-radius: 30rpx;
  border-bottom-right-radius: 30rpx;
  border-top-left-radius: 20rpx; 
  border-top-right-radius: 20rpx;
  
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 30rpx;
  
  /* 🔴 修改：加深阴影，让白色盒子在灰色背景上浮起来，清晰可见 */
  box-shadow: 0 4rpx 16rpx rgba(0,0,0,0.15);
}

/* 搜索输入文字颜色 */
.search-input {
  width: 100%;
  height: 100%;
  font-size: 30rpx;
  /* 🔴 修复：文字改为黑色，背景是白的，这样才看得见 */
  color: #000000; 
  text-align: center;
}

/* 提示文字 */
.search-tip {
  position: absolute;
  right: 60rpx;
  color: #FF3B30;
  font-size: 24rpx;
  font-weight: 600;
}

/* 🆕 上传框样式 */
.upload-box {
  flex: 1; 
  height: 200rpx; 
  background: #F2F2F7;
  border-radius: 16rpx; 
  border: 2rpx dashed #C7C7CC;
  display: flex; 
  flex-direction: column; 
  align-items: center; 
  justify-content: center; 
  overflow: hidden; 
  position: relative;
}

.upload-icon { 
  font-size: 50rpx; 
  margin-bottom: 10rpx; 
}

.upload-text { 
  font-size: 24rpx; 
  color: #999; 
}

.preview-media { 
  width: 100%; 
  height: 100%; 
  object-fit: cover; 
}

/* 左上角关闭按钮样式 */
.dialog-close-x {
  position: absolute;
  top: 20rpx;
  left: 30rpx;
  font-size: 50rpx;
  color: #999;
  line-height: 1;
  font-weight: 300;
  padding: 10rpx; /* 增加一点点击区域 */
  z-index: 1; /* 🔴 修复：降低 z-index，避免遮挡 picker */
}

/* =========================================================================
   🆕 待审核区域样式 (竖向满宽版)
   ========================================================================= */

/* 待审核区域容器 (加高，让它占据大半个屏幕，方便操作) */
/* 管理员待审核区域 (修复滑动和显示问题) */
.pending-section {
  position: fixed;
  top: 180rpx; /* 标题栏下方 */
  left: 0;
  width: 100%;
  /* 🔴 核心修改：高度占满屏幕剩余空间，不要写死 */
  height: calc(100vh - 180rpx); 
  background: rgba(255,255,255,0.98); /* 微透背景 */
  z-index: 50; 
  display: flex;
  flex-direction: column;
  box-shadow: 0 10rpx 50rpx rgba(0,0,0,0.1);
  padding-bottom: 50rpx; /* 底部留白 */
}

.section-header {
  padding: 30rpx 40rpx;
  font-size: 32rpx;
  font-weight: 800;
  color: #000;
  display: flex;
  align-items: baseline;
  background: #fff;
  flex-shrink: 0;
}

/* 滚动区域撑满 */
.pending-scroll {
  flex: 1;
  height: 0; /* 配合flex使用 */
  width: 100%;
}

.pending-list {
  padding: 30rpx 40rpx 100rpx 40rpx; /* 底部加 padding 防止遮挡 */
  display: flex;
  flex-direction: column; /* 🔴 关键：竖向排列 */
  gap: 40rpx; /* 卡片间距 */
}

/* === 满宽大卡片 === */
.pending-card.full-width {
  width: 100%; 
  background: #FFFFFF;
  border-radius: 32rpx;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  box-shadow: 0 8rpx 24rpx rgba(0,0,0,0.06);
}

/* 视频区域 */
.pending-video-large {
  width: 100%;
  height: 400rpx; /* 视频高度加大 */
  background: #000;
}

/* 信息区域 */
.pending-info {
  padding: 30rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.p-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.p-meta {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.p-badges {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  align-items: center;
}

.badge {
  font-size: 22rpx;
  line-height: 1;
  padding: 10rpx 16rpx;
  border-radius: 999rpx;
  font-weight: 700;
}

.badge-blue {
  background: #007AFF;
  color: #fff;
}

.badge-black {
  background: #111;
  color: #fff;
}

.badge-orange {
  background: #FF9500;
  color: #fff;
}

.p-stats {
  display: flex;
  gap: 12rpx;
  flex-wrap: wrap;
}

.stat-pill {
  font-size: 22rpx;
  line-height: 1;
  padding: 10rpx 16rpx;
  border-radius: 999rpx;
  font-weight: 700;
}

.stat-pass {
  background: rgba(0, 200, 83, 0.12);
  color: #00C853;
  border: 2rpx solid rgba(0, 200, 83, 0.35);
}

.stat-reject {
  background: rgba(255, 59, 48, 0.10);
  color: #FF3B30;
  border: 2rpx solid rgba(255, 59, 48, 0.35);
}

.p-date {
  font-size: 26rpx;
  color: #999;
}

.p-name-large {
  font-size: 36rpx;
  font-weight: 700;
  color: #1D1D1F;
}

/* 按钮区域修正 */
.pending-actions-large {
  padding: 20rpx;
  display: flex;
  gap: 20rpx;
  margin-top: auto;
}

.act-btn-lg {
  flex: 1;
  height: 80rpx;
  line-height: 80rpx;
  text-align: center;
  border-radius: 16rpx;
  font-size: 28rpx;
  font-weight: 700;
}

/* 🔴 拒绝按钮 (红色) */
.act-btn-lg.reject {
  background: #FFF0F0;
  color: #FF3B30;
  border: 2rpx solid #FF3B30;
}

/* 🔴 确认/已处理按钮 (绿色 - 对应你的要求) */
.act-btn-lg.pass {
  background: #E8F5E9;
  color: #00C853;
  border: 2rpx solid #00C853;
}

/* 🔴 下载按钮 (黑色) */
.act-btn-lg.download {
  background: #000;
  color: #fff;
}
/* 上传选项弹窗 */
.upload-options {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  margin: 30rpx 0;
}

.option-item {
  display: flex;
  align-items: center;
  padding: 30rpx;
  background: #F5F5F7;
  border-radius: 20rpx;
  transition: all 0.2s;
}

.option-item:active {
  background: #E5E5EA;
  transform: scale(0.98);
}

.option-icon {
  font-size: 48rpx;
  margin-right: 20rpx;
}

.option-text {
  font-size: 28rpx;
  font-weight: 600;
  color: #1D1D1F;
}
