# 弹窗层级（z-index）检查报告

## 📊 当前层级分布

### 1. **最高层级（10000000000+）**
- `.loading-mask` (my页面): **10000000000** ✅ 最高
- `.fill-repair-mask` (填写售后单遮罩): **10000000000** ✅
- `.fill-repair-dialog` (填写售后单内容): **10000000001** ✅
- `.fill-repair-btns` (填写售后单按钮): **10000000002** ✅
- `.fill-repair-btns .cd-btn` (按钮): **10000000003** ✅
- `.auto-toast-mask` (自动提示遮罩): **10000000004** ✅
- `.auto-toast-box` (自动提示内容): **10000000005** ✅

### 2. **高层级（100000000+）**
- `.return-required-mask` (需寄回订单确认遮罩): **100000000** ⚠️
- `.custom-dialog-mask` (自定义对话框遮罩): **1000000001** ⚠️
- `.custom-modal-mask` (自定义弹窗遮罩): **1000000001** ⚠️
- `.custom-dialog-box` (自定义对话框内容): **1000000001** ⚠️

### 3. **中高层级（10000-99999）**
- `.custom-toast .ct-mask` (全局Toast遮罩): **99999** ⚠️
- `.custom-toast .ct-toast-mask` (全局Toast遮罩): **99999** ⚠️
- `.custom-modal-mask` (index/blocked/call页面): **12000** ⚠️
- `.loading-mask` (index页面): **99999** ⚠️
- `.custom-modal` (components): **9999** ⚠️

### 4. **低层级（1000-9999）**
- `.modal` (my页面绑定设备弹窗): **1000** ✅

---

## 🔴 发现的冲突问题

### 问题1: **custom-toast (99999) 低于 auto-toast (10000000004)** ⚠️⚠️⚠️
**位置**: 
- `components/custom-toast/index.wxss`: z-index: 99999
- `pages/my/my.wxss`: auto-toast-mask z-index: 10000000004

**影响**: 
- 如果同时显示 `custom-toast` 和 `auto-toast`，`auto-toast` 会覆盖 `custom-toast`
- 但根据逻辑，`auto-toast` 应该是最高的（因为它是错误提示）

**修复建议**: 
- `custom-toast` 应该低于 `auto-toast`，但高于其他弹窗
- 建议：`custom-toast` 改为 **10000000006**（在 auto-toast 之上，因为它是全局组件）

---

### 问题2: **return-required-mask (100000000) 低于 custom-modal-mask (1000000001)** ⚠️
**位置**:
- `pages/my/my.wxss`: return-required-mask z-index: 100000000
- `pages/my/my.wxss`: custom-modal-mask z-index: 1000000001

**影响**:
- 如果"需寄回订单确认"弹窗打开时，再打开"内容已复制"弹窗，后者会覆盖前者
- 但根据逻辑，"内容已复制"应该是最高的（因为它是临时提示）

**修复建议**: 
- 保持现状，因为"内容已复制"是临时提示，应该在最上层 ✅

---

### 问题3: **index/blocked/call 页面的 custom-modal-mask (12000) 太低** ⚠️⚠️
**位置**:
- `pages/index/index.wxss`: custom-modal-mask z-index: 12000
- `pages/blocked/blocked.wxss`: custom-modal-mask z-index: 12000
- `pages/call/call.wxss`: custom-modal-mask z-index: 12000

**影响**:
- 这些页面的"内容已复制"弹窗可能被其他弹窗覆盖
- 如果页面有其他弹窗（z-index > 12000），会覆盖"内容已复制"

**修复建议**: 
- 统一改为 **1000000001**（和 my 页面一致）

---

### 问题4: **custom-modal 组件 (9999) 太低** ⚠️
**位置**:
- `components/custom-modal/index.wxss`: modal-mask z-index: 9999

**影响**:
- 如果页面有其他弹窗（z-index > 9999），会覆盖 custom-modal

**修复建议**: 
- 改为 **99999**（和 custom-toast 一致，但低于页面级弹窗）

---

### 问题5: **modal (1000) 太低，可能被其他弹窗覆盖** ⚠️
**位置**:
- `pages/my/my.wxss`: .modal z-index: 1000

**影响**:
- 绑定设备弹窗可能被其他弹窗覆盖

**修复建议**: 
- 改为 **10000**（确保在页面内容之上，但低于其他弹窗）

---

## ✅ 建议的层级结构（从高到低）

1. **10000000006**: `custom-toast` (全局Toast，最高优先级)
2. **10000000005**: `auto-toast-box` (自动提示内容)
3. **10000000004**: `auto-toast-mask` (自动提示遮罩)
4. **10000000003**: `fill-repair-btns .cd-btn` (填写售后单按钮)
5. **10000000002**: `fill-repair-btns` (填写售后单按钮区域)
6. **10000000001**: `fill-repair-dialog` (填写售后单内容)
7. **10000000000**: `fill-repair-mask` / `loading-mask` (填写售后单遮罩 / 加载遮罩)
8. **1000000001**: `custom-modal-mask` / `custom-dialog-mask` (页面级弹窗遮罩)
9. **100000000**: `return-required-mask` (需寄回订单确认遮罩)
10. **99999**: `loading-mask` (index页面加载遮罩) / `custom-modal` (组件级弹窗)
11. **10000**: `.modal` (绑定设备弹窗)
12. **1000**: 其他页面内容

---

## 🎯 需要修复的冲突

1. ✅ **custom-toast 层级太低** → 改为 10000000006
2. ✅ **index/blocked/call 页面的 custom-modal-mask 太低** → 改为 1000000001
3. ✅ **custom-modal 组件太低** → 改为 99999
4. ✅ **modal (绑定设备) 太低** → 改为 10000
